<fragment xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:id="@+id/map"
    android:name="com.google.android.gms.maps.SupportMapFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="infozonaorg.com.testnode.MapsActivity">





    <Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center|bottom"
    android:text="Buscar"
    android:padding="10dp"
    android:id="@+id/btnBuscarDisponible" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|bottom"
        android:text="Finalizar"
        android:visibility="gone"
        android:padding="10dp"
        android:id="@+id/btnFinalizarTransaccion" />


    <ImageButton android:id="@+id/btnToggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left|top"
        android:src="@drawable/ic_toggle"
        android:background="@null"></ImageButton>

    <com.dd.processbutton.iml.ActionProcessButton
        android:id="@+id/btnCancelarBusqueda"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|bottom"
        android:padding="10dp"
        android:text="Cancelar Busqueda"
        android:visibility="gone"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        custom:pb_colorComplete="@color/green_complete"
        custom:pb_colorNormal="@color/rojoNormal"
        custom:pb_colorPressed="@color/rojoPresionado"
        custom:pb_colorProgress="@color/rojoNormal"
        custom:pb_textComplete="Cancelado"
        custom:pb_textProgress="Cancelar Busqueda"
        custom:pb_textError="Error"
        custom:pb_colorError="@color/red_error" />

</fragment>
